global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]

  # API /metrics lands in Phase 6. Leave the job commented until then.
  # - job_name: servitor-api
  #   static_configs:
  #     - targets: ["host.docker.internal:8080"]